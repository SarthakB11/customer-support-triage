# Demo video

Built from two sources, stitched with ffmpeg:
- `frames/` — Studio screenshots captured while building the flows (one per step)
- `terminal.cast` — asciinema recording of the curl call (not committed; rendered into the video)

`make-video.sh` turns them into `demo.mp4` (not committed; attached to the GitHub release).
